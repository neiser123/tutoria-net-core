@model List<Amigo>

@{ 

    Layout = "~/Views/share/_Layout.cshtml";
    ViewBag.Title = "AMIGOS INDICE";
}

<h1 style="position: relative;"> lista de objetos</h1>

    @*<table>
        <tr>
            <th>id</th>
            <th>nombre</th>
            <th>email</th>
            <th>ciudad</th>

        </tr>
        <tr>*@
<div class="card-deck">
            @foreach (var amigo in Model)
            {
                var rutafoto = "~/images/" + (amigo.rutaFoto ?? "");
                <div class="card m-3" style="min-width: 14rem; max-width: 14rem;">
                    <div class="card-header">
                        <h3>@amigo.nombre </h3>
                    </div>
                    <img class="card-img-top imagen" src="@rutafoto" asp-append-version="true" />
                    <div class="card-footer text-center">
                        <a class="btn btn-primary" asp-controller="home" asp-action="details" asp-route-id="@amigo.Id" >VER</a>
                        <a class="btn btn-primary" asp-controller="home" asp-action="Edit" asp-route-id="@amigo.Id">EDITAR</a>
                        <a href="#" class="btn btn-danger">BORRAR</a>
                    </div>
                </div>
            }

</div>

        @*</tr>*@
        @*<tr>
            <td>@amigo.Id    </td>
            <td>@amigo.nombre</td>
            <td>@amigo.email </td>
            <td>@amigo.ciudad</td>
        </tr>
    </table>*@

